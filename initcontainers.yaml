apiVersion: v1
kind: Pod
metadata:
  name: initcontainers
spec:
  initContainers:
    - name : initcontainer1
      image: alpine:3
      command:
        - sleep
        - "15"
    - name: initcontainer2
      image: alpine:3
      command:
        - sleep
        - "15"
  containers:
    - name: nginx
      image: nginx:1.23
      ports:
        - containerPort : 80
          protocol: TCP